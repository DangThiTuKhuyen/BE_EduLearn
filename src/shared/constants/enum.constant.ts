export enum GuardEnum {
  AuthGuard = 'AuthGuard',
  UserExistGuard = 'UserExistGuard',
}

export enum RoleValue {
  Administrator = 'administrator',
  User = 'user',
}

export enum Gender {
  Male = 'male',
  Female = 'female',
}
